title: Board

icon: users

options:
  template: false

pages:
  template:
    - person
  sort: true
  build:
    - title: _modules
      uid: modules
      template: modules

files: false

fields:
  title:
    label: Title
    type: title
  modules:
    label: Modules
    type: modules
    template:
